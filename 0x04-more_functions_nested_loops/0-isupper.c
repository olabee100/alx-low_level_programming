#include "main.h"
#include <stdio.h>

/**
 * _isupper - checks for uppercase characte
 * @c: Variable text
 * Return: Always 0.
 */

int main(void)
{
	if (c >= 'A' && c <= 'Z')
	{
		return (1);
	}
	else
	{
		return (0);
	}
}
